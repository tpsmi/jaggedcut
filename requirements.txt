# A Jagged Cut — dependencies
# Python 3.9 or higher.
#
# Notebook 02 starts from precomputed OpenCLIP embeddings, so open_clip_torch
# is not needed; see the companion repository for the extraction step.

numpy>=1.24
pandas>=2.0
matplotlib>=3.7
Pillow>=10.0
scipy>=1.11
scikit-learn>=1.3
umap-learn>=0.5
hdbscan>=0.8
jupyterlab>=4.0

# notebook 01 only
requests>=2.31
tqdm>=4.65
